{{header-component home=true}}
<div>
    {{outlet}}
    <p>Work in progress!!!</p>
    <div class="container">
        <div class="row">
            <div class="col">
                {{#if isLoggedIn}}
                    logged in
                    
                {{else}}
                    not logged in
                 
                {{/if}}
            </div>
        </div>
    </div>
</div>
