<nav class="navbar navbar-expand-lg {{if home "custom-header-landing" "bg-white custom-header"}}">
    <div class="container">
        <div>
            
            {{#link-to 'index' class="navbar-brand"}}
                <span class="{{if home "custom-header-link-landing" "custom-header-link"}}">AppName</span>
            {{/link-to}}
        </div>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item active">
                    {{#link-to 'index' id="home-link" class="nav-link header-link"}}
                        <span class="{{if home "custom-header-link-landing" "custom-header-link"}}">Home</span>
                    {{/link-to}}
                </li>
                <li class="nav-item">
                    {{#link-to 'restaurants' id="restaurants-link" class="nav-link header-link custom-header-link"}}
                        {{#if restaurants}}
                            <span class="custom-header-link-open">Restaurants</span>
                        {{else}}
                            <span class="{{if home "custom-header-link-landing"}}">Restaurants</span>
                        {{/if}}
                    {{/link-to}}
                </li>
                <li class="nav-item">
                    {{#if login}}
                        {{#link-to 'login' id="login-link" class="nav-link header-link custom-header-link-open custom-header-link"}}
                                Login
                        {{/link-to}}
                    {{else}}
                        {{#if currentUser}}
                            <span class="{{if home "custom-header-link-landing" "custom-header-link"}} dropdown">
                                <a href="" data-toggle="dropdown" class="{{if home "custom-header-link-landing" "custom-header-link"}} no-decoration dropdown-toggle nav-link header-link">{{currentUser.firstName}}</a>
                                    <ul class="dropdown-menu">
                                        <li>
                                            {{#if currentUser.admin}}
                                                {{#link-to 'restaurants' id="login-link" class="nav-link header-link custom-header-link"}} 
                                                    Admin Panel
                                                {{/link-to}}
                                            {{else}}
                                                 {{#link-to 'restaurants' id="login-link" class="nav-link header-link custom-header-link"}}
                                                    Account Settings
                                                 {{/link-to}}
                                            {{/if}}
                                        </li>
                                        <li>
                                            {{#link-to 'login' class="nav-link header-link custom-header-link"}} 
                                                <span {{action 'logOut'}}>Logout</span>
                                            {{/link-to}}
                                        </li>
                                    </ul>
                            </span>   
                        {{else}}
                            {{#link-to 'login' class="nav-link header-link"}}
                                <span class="{{if home " custom-header-link-landing " "custom-header-link "}}">
                                    Login
                                </span>
                            {{/link-to}}
                        {{/if}}               
                    {{/if}}
                    
                </li>
            </ul>
        </div>
    </div>
</nav>

{{yield}}